/**
 * content/guidelines.ts
 *
 * Submission guidance — deliberately minimal: submissions must use the
 * given template, full stop. Download links live in content/event.ts
 * under `assets`.
 *
 * Drives: the submission block inside the Evaluation section.
 */

export const templateMandate = {
  title: "Submit on the given template. Only.",
  body: "Download the template, put your idea into it, and upload it through the Google Form. Submissions in any other format are not evaluated — the fixed structure is what makes blind scoring fair.",
};

export const googleSignInNote =
  "The form requires a file upload, so you must be signed in to a Google account to submit. Check your access early — not at 11 PM on deadline night.";
